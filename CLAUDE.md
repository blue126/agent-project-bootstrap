# CLAUDE.md

Read and follow `AGENTS.md`; it is the cross-agent policy source for this repository.

## Claude-specific guidance

- Read `bootstrap-manifest.yml` before changing bootstrap components or workflow selection.
- When validating initialization, use a temporary target and verify that no `origin` remote is invented.
- Keep Claude-only tool, hook, and permission guidance here; do not duplicate shared repository policy from `AGENTS.md`.

